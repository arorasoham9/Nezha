apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Nezha.fullname" . }}-test-connection"
  labels:
    {{- include "Nezha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Nezha.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
